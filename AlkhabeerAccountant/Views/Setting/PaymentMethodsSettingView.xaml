<UserControl x:Class="AlkhabeerAccountant.Views.Setting.PaymentMethodsSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:cc="clr-namespace:AlkhabeerAccountant.CustomControls.SecondaryWindow"
             mc:Ignorable="d"
             FlowDirection="RightToLeft">

    <Grid>
        <!-- Define Rows: main content + bottom buttons -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!-- Main Border Content -->
            <RowDefinition Height="Auto"/>
            <!-- Bottom Buttons -->
        </Grid.RowDefinitions>

        <!-- ===================  Main Border Container =================== -->
        <Border Grid.Row="0" Style="{StaticResource MainCardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!-- ===================  Payment Method Form Section =================== -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                        <!-- Full-width inputs -->
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- اسم وسيلة الدفع -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="اسم وسيلة الدفع" Style="{StaticResource InputLableTextBlock}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}"
                             Style="{StaticResource RoundedTextInputStyle}"  IsEnabled="{Binding IsFormEnabled}"/>

                    <!-- الوصف -->
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="الوصف" 
                               Style="{StaticResource InputLableTextBlock}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description}"
                             Style="{StaticResource RoundedTextInputStyle}"    IsEnabled="{Binding IsFormEnabled}"/>

                    <!-- الحالة -->
                    <cc:StatusSelector Grid.Row="5"
                                       Grid.Column="0"
                                       Grid.ColumnSpan="2"
                                       IsEnabled="{Binding IsFormEnabled}"
                                       IsActive="{Binding IsActive, Mode=TwoWay}" />

                </Grid>

                <!-- ===================  Payment Methods Table Section =================== -->
                <Border Grid.Row="1" Style="{StaticResource TableBorderStyle}">
                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                              Style="{StaticResource TableStyle}"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              LoadingRow="DataGrid_LoadingRow">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="اسم وسيلة الدفع" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="ملاحظات" Binding="{Binding Description}" Width="*"/>
                            <DataGridTextColumn Header="الحالة" Binding="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}" 
                                                CellStyle="{StaticResource StatusCellStyle}" Width="70"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
                <cc:PaginationControl Grid.Row="2" DataContext="{Binding}" />
            </Grid>
        </Border>
        <!-- ===================  Bottom Buttons Outside Border =================== -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" >
            <Button Command="{Binding SaveOrUpdateCommand}"
        Style="{StaticResource SaveButtonStyle}"
        IsEnabled="{Binding IsSaveEnabled}"/>

            <Button Command="{Binding AddCommand}"
        Style="{StaticResource AddButtonStyle}"/>

            <Button Command="{Binding EditCommand}"
        Style="{StaticResource EditButtonStyle}"
        IsEnabled="{Binding IsEditEnabled}"/>

            <Button Command="{Binding DeleteCommand}"
        Style="{StaticResource DeleteButtonStyle}"
        IsEnabled="{Binding IsDeleteEnabled}"/>

        </StackPanel>
    </Grid>
</UserControl>
