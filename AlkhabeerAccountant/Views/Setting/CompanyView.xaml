<UserControl x:Class="AlkhabeerAccountant.Views.Setting.CompanyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 🔹 Form Container -->
        <Border Grid.Row="0" Style="{StaticResource MainCardBorderStyle}">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 🏢 اسم الشركة -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="اسم الشركة"  VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="0" Grid.Column="1" />

                <!-- 🏠 عنوان الشركة -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="عنوان الشركة"  VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="1" Grid.Column="1" />

                <!-- 💼 النشاط -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="النشاط"  VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="2" Grid.Column="1" />

                <!-- ✉️ البريد الإلكتروني -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="البريد الإلكتروني"  VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="3" Grid.Column="1" />

                <!-- 🌐 الويب سايت -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="الويب سايت"  VerticalAlignment="Center" Grid.Row="4" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="4" Grid.Column="1" />

                <!-- 💳 الرقم الضريبي -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="الرقم الضريبي"  VerticalAlignment="Center" Grid.Row="5" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="5" Grid.Column="1" />

                <!-- 🧾 السجل التجاري -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}"  
                           Text="السجل التجاري"  VerticalAlignment="Center" Grid.Row="6" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="6" Grid.Column="1" />

                <!-- 📱 رقم الموبايل -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="رقم الموبايل"  VerticalAlignment="Center" Grid.Row="7" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Grid.Row="7" Grid.Column="1" />

                <!-- 🧾 السطر الأخير في فاتورة المبيعات -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="السطر الأخير في فاتورة المبيعات"  VerticalAlignment="Center" Grid.Row="8" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Text="Thank You For Purchasing" Grid.Row="8" Grid.Column="1" />

                <!-- 📝 نص يظهر في آخر فاتورة المبيعات -->
                <TextBlock  Style="{StaticResource InputLableTextBlock}" Text="نص يظهر في آخر فاتورة المبيعات"  VerticalAlignment="Center" Grid.Row="9" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedTextInputStyle}" Text="(يسمح بإرجاع المنتجات خلال 14 يوم)" Grid.Row="9" Grid.Column="1" />
            </Grid>
        </Border>

        <!-- 🔹 Save Button -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Style="{StaticResource SaveButtonStyle}"
                    Content="حفظ"/>
        </StackPanel>
    </Grid>
</UserControl>
