<UserControl x:Class="AlkhabeerAccountant.Views.Setting.CompanyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 🔹 Form Container -->
        <Border Grid.Row="0"
                CornerRadius="10"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Background="{StaticResource CardBackgroundBrush}"
                Padding="25">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 🏢 اسم الشركة -->
                <TextBlock Text="اسم الشركة" Margin="10" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="0" Grid.Column="1" Margin="10"/>

                <!-- 🏠 عنوان الشركة -->
                <TextBlock Text="عنوان الشركة" Margin="10" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="1" Grid.Column="1" Margin="10"/>

                <!-- 💼 النشاط -->
                <TextBlock Text="النشاط" Margin="10" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="2" Grid.Column="1" Margin="10"/>

                <!-- ✉️ البريد الإلكتروني -->
                <TextBlock Text="البريد الإلكتروني" Margin="10" VerticalAlignment="Center" Grid.Row="3" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="3" Grid.Column="1" Margin="10"/>

                <!-- 🌐 الويب سايت -->
                <TextBlock Text="الويب سايت" Margin="10" VerticalAlignment="Center" Grid.Row="4" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="4" Grid.Column="1" Margin="10"/>

                <!-- 💳 الرقم الضريبي -->
                <TextBlock Text="الرقم الضريبي" Margin="10" VerticalAlignment="Center" Grid.Row="5" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="5" Grid.Column="1" Margin="10"/>

                <!-- 🧾 السجل التجاري -->
                <TextBlock Style="{StaticResource InputLableTextBlock}"  Margin="10"
                           Text="السجل التجاري"  VerticalAlignment="Center" Grid.Row="6" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="6" Grid.Column="1" Margin="10"/>

                <!-- 📱 رقم الموبايل -->
                <TextBlock Text="رقم الموبايل" Margin="10" VerticalAlignment="Center" Grid.Row="7" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Grid.Row="7" Grid.Column="1" Margin="10"/>

                <!-- 🧾 السطر الأخير في فاتورة المبيعات -->
                <TextBlock Text="السطر الأخير في فاتورة المبيعات" Margin="10" VerticalAlignment="Center" Grid.Row="8" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Text="Thank You For Purchasing" Grid.Row="8" Grid.Column="1" Margin="10"/>

                <!-- 📝 نص يظهر في آخر فاتورة المبيعات -->
                <TextBlock Text="نص يظهر في آخر فاتورة المبيعات" Margin="10" VerticalAlignment="Center" Grid.Row="9" Grid.Column="0"/>
                <TextBox Style="{StaticResource RoundedInputStyle}" Text="(يسمح بإرجاع المنتجات خلال 14 يوم)" Grid.Row="9" Grid.Column="1" Margin="10"/>
            </Grid>
        </Border>

        <!-- 🔹 Save Button -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Style="{StaticResource SaveButtonStyle}"
                    Content="حفظ"/>
        </StackPanel>
    </Grid>
</UserControl>
