<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="BlueRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="FontSize" Value="{StaticResource FontSize-3}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Outer circle -->
                        <Border x:Name="OuterCircle"
                                Width="20" Height="20"
                                CornerRadius="10"
                                BorderThickness="2"
                                BorderBrush="#1D71FF"
                                VerticalAlignment="Center"
                                Margin="0,0,6,0"
                                Background="Transparent">
                            <!-- Inner filled circle -->
                            <Ellipse x:Name="InnerDot"
                                     Width="10" Height="10"
                                     Fill="#1D71FF"
                                     Visibility="Collapsed"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"/>
                        </Border>

                        <!-- Label -->
                        <ContentPresenter VerticalAlignment="Center" />
                    </StackPanel>

                    <!-- Visual states -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="InnerDot" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterCircle" Property="Background" Value="#E6EEFF"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterCircle" Property="BorderBrush" Value="#B0BEC5"/>
                            <Setter TargetName="InnerDot" Property="Fill" Value="#B0BEC5"/>
                            <Setter Property="Foreground" Value="#9E9E9E"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>