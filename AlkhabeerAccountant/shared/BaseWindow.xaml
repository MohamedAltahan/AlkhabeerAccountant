<Window x:Class="AlkhabeerAccountant.shared.BaseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlkhabeerAccountant.shared"
        mc:Ignorable="d" 
        xmlns:wc="clr-namespace:System.Windows.Shell;assembly=PresentationFramework">

    <Window.Resources>
        <!-- ✅ Base style for all custom windows -->
        <Style x:Key="BaseWindowStyle" TargetType="Window">
            <Setter Property="WindowStyle" Value="None"/>
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="AllowsTransparency" Value="False"/>

            <!-- Remove system padding and caption -->
            <Setter Property="wc:WindowChrome.WindowChrome">
                <Setter.Value>
                    <wc:WindowChrome 
                        CaptionHeight="0"
                        CornerRadius="0"
                        GlassFrameThickness="0"
                        ResizeBorderThickness="0"
                        UseAeroCaptionButtons="False" />
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    <Grid>

    </Grid>
</Window>
